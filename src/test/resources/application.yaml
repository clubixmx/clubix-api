spring:
  application:
    name: clubix-api
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  flyway:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    user: sa
  ai:
    openai:
      api-key: test-key   # dummy — OrchestratorService está mockeado, nunca se llama
  data:
    redis:
      host: localhost
      port: 6379

whatsapp:
  hook:
    verify-token: test-token
  outgoing:
    meta-url: http://localhost:9999
    token: test-token
